---
import { site } from "@/consts";
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<h1
  class={cn(
    "font-extrabold tracking-tight text-zinc-900 site-title",
    className,
  )}
>
  <a href="/">{site.author}</a>
</h1>
