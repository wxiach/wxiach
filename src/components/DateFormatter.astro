---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  date: Date;
}

const { class: className, date } = Astro.props as Props;

const options: Intl.DateTimeFormatOptions = { month: "short", day: "numeric", year: "numeric" };
---

<span class={cn(className, "text-xs font-normal text-zinc-400 tabular-nums tracking-tight")}
  >{date.toLocaleDateString("en-US", options)}</span
>
